function ConvertToHtml(json) {
    //txtsearch.value = json;
    //$('#txtsearch').val(json);
    ////alert(json)

    var Data = JSON.parse(json);
    //divdrugslist.innerHTML = drugs.length;

    pg18.innerHTML = "Change";
    //txtsearch.value = html;
    //list1.innerHTML = html;

    //$(drugs).each(function (idx, drug) {
    //})
}
